#include <stdio.h>

int main(int argc, char const *argv[])
{
	//int limit_of_int = 4294967295;
	int swap_from_positif_to_negatif = 2147483647;
	int add = 1;
	printf("last int = %d\n", swap_from_positif_to_negatif);
	int after_limit = swap_from_positif_to_negatif +add;
	printf("after haffa = %d\n", after_limit);
	return 0;
}